import React from 'react';

interface ITeacherInfo {
  fullName: string;
}

export default function ClassCard({
  subject,
  teacher,
}: {
  subject: string;
  teacher: string;
}) {
  return (
    <div>
      <div></div>
    </div>
  );
}
